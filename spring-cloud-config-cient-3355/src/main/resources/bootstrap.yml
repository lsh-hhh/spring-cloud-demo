spring:
  cloud:
    config:
      name: application-config-client # 无后缀名.yml or properties
      profile: dev # 本次访问的配置环境
      label: master
      uri: http://springconig-3344.com:3344 # spring-cloud-config服务映射地址，本次微服务启动，先访问它获取配置文件
